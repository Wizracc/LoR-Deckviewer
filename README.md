# LoR-Deckviewer

This project's structure is such that the "server" folder contains the built frontend and the express/node backend/server. The lor-deck-viewer folder contains the source for the built frontend.
